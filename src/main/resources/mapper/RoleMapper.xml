<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dorohedoro.mapper.RoleMapper">

    <resultMap id="baseResultMap" type="role">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="role_name" jdbcType="VARCHAR" property="authority" />
    </resultMap>
    
    <select id="selectByUsername" resultMap="baseResultMap">
        select roles.id, roles.role_name from users_roles join users on users_roles.user_id = users.id join roles on users_roles.role_id = roles.id where users.username = #{username};
    </select>
</mapper>
